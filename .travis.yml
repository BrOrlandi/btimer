language: node_js
node_js:
  - "10"

script:
  - npm ci
  - npm run lint
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  keep-history: true
  on:
    branch: master